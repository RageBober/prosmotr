{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Права главного окна: только диалог выбора папки. Файлы читает Rust, интерфейсу доступа к диску не выдаём.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
